Updates this commit:

	TYP DESCRIPTION
	-------------------
	 +  Add support for 4 more keyboard inputs (';', ':', '/', '?')
	 +	Add the ability to parse hexadecimal numbers from Strings
	 +  Add HexEditor (currently only displays sectors from the detected AHCI drive)
	 +	Add Minnow5 (a new, actually functional this time, filesystem)
	 *	Update iConsole2 to use Minnow5, add commands to reflect this
	 *	Make iConsole2 EchoDec display a negative sign if needed
	 *	Fix iConsole2 not allowing backspaces after running the clear command
	 *	Update FileChooser to use Minnow5 (is still unable to swap directories)
	 *	Update TextEditor to use Minnow5
	 *	Make TextEditor use an appropriate buffer size when writing out to a file
	 *	Update ImageEditor to use Minnow5
	 *	Update bfParse to use Minnow5
	 *	Stop ps2 waitForData and waitForWrite from trashing the debug call stacks
	 -	Temporarily disable 'FiletypeBinding's
	 -	Remove time delay on boot
	 -	Disable paging
	 

Issues affected by this commit:

	NUM   ACTION     DESCRIPTION
	--------------------------------
	
